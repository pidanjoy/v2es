import 'package:flutter/material.dart';

class TopicLoadingWidget extends StatelessWidget {
  const TopicLoadingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("加载中...");
  }
}
